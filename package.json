{
  "name": "whatsapp-direct",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy ./public",
    "build": "npm install"
  },
  "dependencies": {
    "googleapis": "^128.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.71.0"
  }
}